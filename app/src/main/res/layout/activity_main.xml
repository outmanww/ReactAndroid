<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.optimind_react.reactadroid.MainActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/button_react_string"
        android:onClick="reactButtonClick"
        android:id="@+id/reactButton"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:background="@color/colorReact" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/push_button_string"
        android:id="@+id/currentReactText"
        android:layout_above="@+id/reactButton"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="56dp"
        android:inputType="text"
        android:width="350dp"
        android:textAlignment="center"
        android:textSize="@dimen/abc_text_size_medium_material" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/clickNumText"
        android:inputType="text"
        android:width="350dp"
        android:textAlignment="center"
        android:textSize="@dimen/abc_text_size_medium_material"
        android:layout_marginTop="46dp"
        android:layout_below="@+id/reactButton"
        android:layout_centerHorizontal="true" />
</RelativeLayout>
